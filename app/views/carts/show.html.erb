<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>
      <%= render 'fullcart' if enhanced_cart.length > 0 %>
      <%= render 'emptycart' if enhanced_cart.length == 0 %>
</section>
